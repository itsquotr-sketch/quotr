import { FileText } from "lucide-react";
import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function QuotesPage() {
  return (
    <PlaceholderPage
      title="Quotes"
      description="Build and send client quotes from your project estimates."
      icon={FileText}
    />
  );
}
