import { Layers } from "lucide-react";
import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function AssembliesPage() {
  return (
    <PlaceholderPage
      title="Assemblies"
      description="Build reusable item groups for faster estimating."
      icon={Layers}
    />
  );
}
